# Copy this file to ".env" in the repo root (same folder as docker-compose.yml).
# Docker Compose reads ".env" for variable substitution in docker-compose.yml.
#
# Must be identical to INTERNAL_API_KEY in ass/.env
PYTHON_INTERNAL_API_KEY=

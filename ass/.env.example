# Copy to ass/.env before: docker compose up (docker-compose mounts this file)
OPENAI_API_KEY=
INTERNAL_API_KEY=
# Browser origin(s) hitting your site, comma-separated, no spaces
# Single EC2 + nginx: use http://YOUR_PUBLIC_IP  or  https://your-domain.com
ALLOWED_ORIGINS=http://localhost:3000
